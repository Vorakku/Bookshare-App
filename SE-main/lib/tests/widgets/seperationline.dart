import 'package:flutter/material.dart';

class Seperationline extends StatelessWidget {
  const Seperationline({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        height: 2.5,
        width: double.infinity,
        color: Colors.black,
      );
  }
}
